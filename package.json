{
  "name": "hara-control",
  "version": "0.1.16",
  "private": true,
  "description": "Apache-2.0 self-hosted control plane for the Hara fleet.",
  "license": "Apache-2.0",
  "type": "commonjs",
  "scripts": {
    "build": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\" && tsc -p tsconfig.build.json",
    "start": "node dist/main.js",
    "start:dev": "tsx watch src/main.ts",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:deploy": "node scripts/recover-known-failed-migration.mjs && prisma migrate deploy",
    "admin": "tsx cli/admin.ts",
    "provider-secret": "node dist/ops/provider-secret.js",
    "test": "node --test --import tsx test/*.test.ts"
  },
  "dependencies": {
    "@nestjs/common": "^11.1.28",
    "@nestjs/core": "^11.1.28",
    "@nestjs/platform-express": "^11.1.28",
    "@prisma/client": "^6.2.0",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.1",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "@nestjs/testing": "^11.1.28",
    "@types/express": "^5.0.0",
    "@types/node": "^22.10.0",
    "prisma": "^6.2.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  },
  "overrides": {
    "fast-uri": "3.1.4",
    "js-yaml": "4.3.0"
  }
}
